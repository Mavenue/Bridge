<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bridge.sys.mapper.TopStructureDetectionResultMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.bridge.sys.pojo.TopStructureDetectionResult">
        <id column="bridge_id" property="bridgeId" />
        <result column="periodic_detection_date" property="periodicDetectionDate" />
        <result column="top_structure_damage_type_id" property="topStructureDamageTypeId" />
        <result column="top_structure_span_num" property="topStructureSpanNum" />
        <result column="top_structure_deduction" property="topStructureDeduction" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        bridge_id, periodic_detection_date, top_structure_damage_type_id, top_structure_span_num, top_structure_deduction
    </sql>

</mapper>
